<div class="container my-5"></div>
  <div class="row mt-5">
    <div class="col-9">
      <div class="d-flex justify-content-between">
        <h1>Book <%= @listing.mascot_name %> for your event</h1>
        <%= link_to listing_path(@listing) do %>
          <button class="btn btn-secondary">Back to mascot's details</button>
        <% end %>
      </div>
    </div>
  </div>
  <div class="row row-col-6">
    <div class="col-9">
      <%= render 'form', article: [@listing, @booking] %>
    </div>
  </div>


  <div class="row m-5">
    <%# <%= link_to listing_booking_path(@listing) do %>
      <%# <button type="button" class="btn btn-primary">Back to Mascot</button> %>
    <%# <% end %>
  </div>
</div>
